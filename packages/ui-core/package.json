{
  "name": "polkadot-ui-core",
  "license": "MIT",
  "version": "2.0.2",
  "description": "Core styles and themes for Polkadot dApps.",
  "keywords": [
    "Polkadot",
    "CSS",
    "UI"
  ],
  "bugs": {
    "url": "https://github.com/polkadot-ui/library/issues"
  },
  "homepage": "https://github.com/polkadot-ui/library#readme",
  "scripts": {
    "build:mock": "node ../../builder/run.mjs -t package:build -p ui-core",
    "build": "rm -fr dist && gulp && yarn build:mock",
    "watch": "rm -fr dist && gulp --silent && gulp --silent --t watch",
    "clear": "rm -rf node_modules dist tsconfig.tsbuildinfo"
  }
}
